///[имя кнопки] [*] Всплывающая подсказка
%standart%    // настройки кнопки для режима «Стандартный» 
    {A} имя набора | имя операции
    {A} * | имя операции
%expanded%    // настройки кнопки для режима «Расширенный» 
    {A} имя набора | имя операции
    //{A} * | имя операции

[имя кнопки] [green] Всплывающая подсказка
%standart%
    {A} имя набора | имя операции
    {S} имя скрипта
%expanded%
    {A} имя набора | имя операции
    {A} * | имя операции

[имя кнопки] [blue] Всплывающая подсказка
%standart%
    {A} имя набора | имя операции
    {A} * | имя операции
%expanded%
    {A} имя набора | имя операции
    {S} имя скрипта
    {A} * | имя операции

[имя кнопки] [orange] Всплывающая подсказка
%standart%
    {A} имя набора | имя операции
    {A} * | имя операции
%expanded%
    {A} имя набора | имя операции
    {A} * | имя операции

[имя кнопки] [yellow] Всплывающая подсказка
%standart%
    {A} имя набора | имя операции
    {A} * | имя операции
%expanded%
    {A} имя набора | имя операции
    {A} * | имя операции

[имя кнопки] [gray] Всплывающая подсказка
%standart%
    {A} имя набора | имя операции
    {A} * | имя операции
%expanded%
    {A} имя набора | имя операции
    {A} * | имя операции